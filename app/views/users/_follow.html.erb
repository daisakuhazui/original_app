<%= form_for(current_user.followers.build(follower_id: @user.id)) do |f| %>
  <div>
    <%= f.hidden_field_tag :follower_id, @user.id %>
  </div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>
